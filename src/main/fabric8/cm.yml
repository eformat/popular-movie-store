data:
  application.yaml: |-
    moviestore:
      apiEndpointUrl: "https://api.themoviedb.org"
      apiKey: "${API_KEY}"
    infinispan:
      embedded:
        config-xml: infinispan-cassandra-moviestore.xml